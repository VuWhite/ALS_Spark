services:
  spark-master:
    image: apache/spark:3.5.1
    container_name: spark-master
    environment:
      - SPARK_MODE=master
    ports:
      - "8080:8080"
      - "7077:7077"
      - "4040:4040"
    volumes:
      - spark-master:/opt/spark
      - ./data:/data
    command: >
      /opt/spark/bin/spark-class org.apache.spark.deploy.master.Master
      --host spark-master
      --port 7077
      --webui-port 8080
    mem_limit: 3g
    cpus: 1.0
    networks:
      - spark-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080"]
      interval: 10s
      timeout: 5s
      retries: 5

  spark-worker-1:
    image: apache/spark:3.5.1
    container_name: spark-worker-1
    environment:
      - SPARK_MODE=worker
      - SPARK_MASTER_URL=spark://spark-master:7077
      - SPARK_WORKER_MEMORY=6g
      - SPARK_WORKER_CORES=1
    volumes:
      - spark-worker-1:/opt/spark
      - ./data:/data
    command: >
      /opt/spark/bin/spark-class org.apache.spark.deploy.worker.Worker
      spark://spark-master:7077
      --webui-port 8081
    mem_limit: 6g
    cpus: 1.0
    depends_on:
      spark-master:
        condition: service_healthy
    networks:
      - spark-network

  spark-worker-2:
    image: apache/spark:3.5.1
    container_name: spark-worker-2
    environment:
      - SPARK_MODE=worker
      - SPARK_MASTER_URL=spark://spark-master:7077
      - SPARK_WORKER_MEMORY=6g
      - SPARK_WORKER_CORES=1
    volumes:
      - spark-worker-2:/opt/spark
      - ./data:/data
    command: >
      /opt/spark/bin/spark-class org.apache.spark.deploy.worker.Worker
      spark://spark-master:7077
      --webui-port 8081
    mem_limit: 6g
    cpus: 1.0
    depends_on:
      spark-master:
        condition: service_healthy
    networks:
      - spark-network

  spark-worker-3:
    image: apache/spark:3.5.1
    container_name: spark-worker-3
    environment:
      - SPARK_MODE=worker
      - SPARK_MASTER_URL=spark://spark-master:7077
      - SPARK_WORKER_MEMORY=6g
      - SPARK_WORKER_CORES=1
    volumes:
      - spark-worker-3:/opt/spark
      - ./data:/data
    command: >
      /opt/spark/bin/spark-class org.apache.spark.deploy.worker.Worker
      spark://spark-master:7077
      --webui-port 8081
    mem_limit: 6g
    cpus: 1.0
    depends_on:
      spark-master:
        condition: service_healthy
    networks:
      - spark-network

  spark-worker-4:
    image: apache/spark:3.5.1
    container_name: spark-worker-4
    environment:
      - SPARK_MODE=worker
      - SPARK_MASTER_URL=spark://spark-master:7077
      - SPARK_WORKER_MEMORY=6g
      - SPARK_WORKER_CORES=1
    volumes:
      - spark-worker-4:/opt/spark
      - ./data:/data
    command: >
      /opt/spark/bin/spark-class org.apache.spark.deploy.worker.Worker
      spark://spark-master:7077
      --webui-port 8081
    mem_limit: 6g
    cpus: 1.0
    depends_on:
      spark-master:
        condition: service_healthy
    networks:
      - spark-network

  spark-worker-5:
    image: apache/spark:3.5.1
    container_name: spark-worker-5
    environment:
      - SPARK_MODE=worker
      - SPARK_MASTER_URL=spark://spark-master:7077
      - SPARK_WORKER_MEMORY=6g
      - SPARK_WORKER_CORES=1
    volumes:
      - spark-worker-5:/opt/spark
      - ./data:/data
    command: >
      /opt/spark/bin/spark-class org.apache.spark.deploy.worker.Worker
      spark://spark-master:7077
      --webui-port 8081
    mem_limit: 6g
    cpus: 1.0
    depends_on:
      spark-master:
        condition: service_healthy
    networks:
      - spark-network

volumes:
  spark-master:
  spark-worker-1:
  spark-worker-2:
  spark-worker-3:
  spark-worker-4:
  spark-worker-5:

networks:
  spark-network:
    driver: bridge
